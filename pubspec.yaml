name: check_list_stress
description: ADM Pilot Assessment System – IMSAFE + PAVE + DECIDE
version: 2.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Existing – unchanged
  flutter_animate: ^4.5.0
  google_fonts: ^6.2.1
  camera: ^0.10.0
  google_mlkit_face_detection: ^0.7.0
  share_plus: ^7.0.0
  url_launcher: ^6.0.0
  pdf: ^3.10.8
  printing: ^5.12.0
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  intl:
  path_provider: ^2.1.3
  image_picker: ^1.1.2
  fl_chart: ^0.68.0

  # NEW – Storage (Hive)
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # NEW – Routing
  go_router: ^13.2.0

  # NEW – Utils
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  # NEW – Hive code generation
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  generate: true
